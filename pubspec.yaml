name: alumni_mentorship_platform
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  go_router: ^17.3.0
  google_fonts: ^8.1.0
  json_annotation: ^4.12.0
  supabase_flutter: ^2.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  json_serializable: ^6.14.0
flutter:
  uses-material-design: true
  assets:
    - .env
